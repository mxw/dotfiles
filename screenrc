# shell -$SHELL
# term xterm
# setenv SSH_AUTH_SOCK "$HOME/.authsock-$HOSTNAME"

# screen -t main 0 screenup

startup_message off

altscreen on
defscrollback 10000

hardstatus alwayslastline "%{= KC}[%H]   %{g}%?%-Lw%?%{M}(%{b}%n* %f%t%?(%u)%?%{M})%{g}%?%+Lw%?%? %= %l    %m-%d-%Y %c:%s"
